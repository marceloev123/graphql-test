query getCharacterById($characterId: ID!) {
  character(id: $characterId) {
    id
    name
    status
    species
    type
    gender
    image
    origin {
      name
    }
    location {
      name
    }
  }
}
